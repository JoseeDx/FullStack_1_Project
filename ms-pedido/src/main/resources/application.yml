server:
  port: 9094

spring:
  application:
    name: ms-pedido
    
  datasource:
    # Cambiado a host.docker.internal
    url: jdbc:mysql://host.docker.internal:3306/db_pedido?createDatabaseIfNotExist=true
    username: root
    password: 

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog.sql